diff --git a/app/(protected)/app/projects/page.tsx b/app/(protected)/app/projects/page.tsx
index d868b1f..0df7262 100644
--- a/app/(protected)/app/projects/page.tsx
+++ b/app/(protected)/app/projects/page.tsx
@@ -373,6 +373,11 @@ export default async function ProjectsPage({
           select: { seenAt: true },
           take: 1,
         },
+        // ✅ Assigned workers (for admin/manager/BD view)
+        assignments: {
+          where: { unassignedAt: null },
+          select: { user: { select: { fullName: true } } },
+        },
       },
     }),
   ]);
@@ -468,6 +473,11 @@ export default async function ProjectsPage({
                       {p.createdBy.fullName} • {p.department.name}
                       {p.bdOwner?.fullName ? ` • BD: ${p.bdOwner.fullName}` : ""}
                     </div>
+                    {canEditFinancials && p.assignments?.length > 0 && (
+                      <div className="text-xs text-muted-foreground truncate mt-0.5">
+                        👤 {p.assignments.map((a: any) => a.user.fullName).join(", ")}
+                      </div>
+                    )}
                     {isUnread && (
                       <form action={markProjectSeenAction} className="mt-1">
                         <input type="hidden" name="projectId" value={p.id} />
